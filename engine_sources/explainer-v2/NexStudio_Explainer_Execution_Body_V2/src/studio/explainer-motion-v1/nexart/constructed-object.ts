import type { ProductionRenderBinding } from "../types";
/** Retained only as a compatibility symbol. Canned prose->object inference is forbidden. */
export function constructedObjectBinding(_entityId:string,_semantic:string):ProductionRenderBinding|undefined{return undefined;}
